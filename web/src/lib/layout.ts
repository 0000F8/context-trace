/**
 * Shared layout constants for the trace view's instrument panel.
 * The composition timeline and the strata grid both use these so their
 * columns line up into one x-axis grid (the "core sample" concept).
 */
export const COLUMN_WIDTH = 40;
export const ROW_LABEL_WIDTH = 168;
export const TIMELINE_HEIGHT = 168;
export const ROW_HEIGHT = 26;
export const AXIS_HEIGHT = 24;
