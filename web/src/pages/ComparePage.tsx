/** Placeholder — implemented per spec2 §F by the compare workstream. */
export function ComparePage() {
  return (
    <div style={{ padding: 32 }}>
      <p>Comparison view is being assembled.</p>
    </div>
  );
}
